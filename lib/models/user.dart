//if we have anonymous user in out application firebase return unique id which
//we can use to utilize to save ooperation of user and idetify user

class UserModel{
  String uid;
  UserModel(this.uid);
}